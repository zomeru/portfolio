import React from 'react';
import { Nav } from '../index';

interface IHeroProps {}

const Hero: React.FC<IHeroProps> = ({}) => {
  return (
    <div>
      <Nav />
    </div>
  );
};

export default Hero;
