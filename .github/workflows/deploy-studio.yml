name: Deploy Sanity Studio

on:
  push:
    branches: [main, dev]
    paths:
      - "apps/studio/**"
      - "packages/env/**"
      - "packages/typescript-config/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "turbo.json"
      - ".github/workflows/deploy-studio.yml"

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name == 'main' && 'Production' || 'Preview' }}
    env:
      SANITY_API_TOKEN: ${{ secrets.SANITY_API_TOKEN }}
      NEXT_PUBLIC_SANITY_APP_ID: ${{ vars.NEXT_PUBLIC_SANITY_APP_ID }}
      NEXT_PUBLIC_SANITY_DATASET: ${{ github.ref_name == 'main' && 'production' || 'development' }}
      NEXT_PUBLIC_SANITY_PROJECT_ID: ${{ vars.SANITY_STUDIO_PROJECT_ID }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
      
      - name: Setup pnpm (includes Node.js + cache)
        uses: pnpm/setup@v1
        with:
          cache: true
          cache-dependency-path: pnpm-lock.yaml
          install: false

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type Check
        run: pnpm --filter @portfolio/studio check-types

      - name: Build
        run: pnpm --filter @portfolio/studio build

      - name: Deploy
        run: pnpm --filter @portfolio/studio deploy --yes
